# Survey-Data-Analysis-using-K-Means-Clustering-
This project applies the K-Means Clustering algorithm to analyze survey data and identify meaningful groups based on similarities in the responses. Since the dataset has no predefined labels, an unsupervised machine learning approach is used to discover hidden patterns.
